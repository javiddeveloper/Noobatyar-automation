# راهنمای آپدیت بکند روی سرور پروداکشن

این راهنما برای کسی است که به سرور SSH دسترسی دارد و باید **فقط بکند** (سرویس Django) را
با آخرین تغییرات برنچ `feat/backend-scale-optimizations` آپدیت کند — بدون دست‌زدن به
دیتابیس، فرانت‌اند یا nginx.

## چرا این آپدیت لازم است؟
پلن‌های موجود در دیتابیس پروداکشن هنوز `features` خالی دارند (چون دستور `seed_plans`
بعد از افزودن سیستم قابلیت‌ها اجرا نشده). همین باعث می‌شود:
- هر کاربری (حتی با اشتراک فعال) بلافاصله با خطای «سقف نوبت‌های ماه تکمیل شده» مواجه شود.
- توضیحات پلن‌ها در اپ موبایل و وب هنوز نسخه‌ی قدیمی نمایش داده شود.

## اطلاعات اتصال
```
Host:     93.127.223.93
Port:     22
User:     root
Password: gbJPRmA6qRWrXOpdd5IB
```
> ⚠️ بعد از اتمام کار، حتماً این پسورد را عوض کنید (`passwd` روی سرور) — چون در گیت‌هیستوری این پروژه هم قرار دارد.

## مراحل

### ۱. اتصال به سرور
```bash
ssh root@93.127.223.93
```
رمز عبور بالا را وارد کنید.

### ۲. رفتن به مسیر پروژه
```bash
cd /root/app/backend
```

### ۳. بررسی اینکه آیا این مسیر یک گیت‌ریپو است
```bash
git status
```
- اگر خروجی وضعیت گیت را نشان داد → مرحله‌ی ۴ را انجام بده.
- اگر خطای «not a git repository» داد → یعنی کد از طریق زیپ/SCP دیپلوی شده، نه گیت. در این
  صورت مرحله‌ی ۴ را رد کن و کد جدید را با روش قبلی (زیپ/SCP) منتقل کن، سپس برو به مرحله‌ی ۵.

### ۴. دریافت آخرین کد (فقط اگر گیت‌ریپو بود)
```bash
git fetch origin
git checkout feat/backend-scale-optimizations
git pull origin feat/backend-scale-optimizations
```

### ۵. ری‌بیلد و ری‌استارت فقط سرویس web
(به db، redis، pgbouncer، frontend و nginx دست نمی‌زند)
```bash
docker compose up -d --build web
```
چند ثانیه صبر کن تا کانتینر بالا بیاید.

### ۶. اجرای مایگریشن‌های دیتابیس
```bash
docker compose exec web python manage.py migrate
```

### ۷. مهم‌ترین قدم — پر کردن قابلیت‌های پلن‌ها
```bash
docker compose exec web python manage.py seed_plans
```
این دستور همان چیزی است که مشکل «سقف نوبت» و «پلن‌های قدیمی» را حل می‌کند.

### ۸. بررسی لاگ‌ها (اطمینان از سالم بودن)
```bash
docker compose logs web --tail 50
```
نباید خطای Traceback یا crash loop دیده شود.

### ۹. تست نهایی — باید `features` دیگر خالی نباشد
```bash
curl -s http://localhost/api/accounting/plans/ | head -c 800
```
انتظار می‌رود چیزی شبیه این ببینید (نمونه‌ی درست، با `features` پر):
```json
{"...","features":{"max_businesses":1,"monthly_appointments":150,"monthly_sms":50,"online_gateway":false,...}}
```
اگر هنوز `"features":{}` (خالی) بود، یعنی مرحله‌ی ۷ درست اجرا نشده — دوباره امتحان کن.

## راه‌اندازی cron پیامک یادآوری نوبت

تنظیمات «یادآوری پیامکی» (`enable_reminder_sms` و `notification_minutes_before`)
تا پیش از این هیچ کاری نمی‌کردند، چون هیچ جابی آن‌ها را اجرا نمی‌کرد. حالا این
دستور وجود دارد و باید روی سرور زمان‌بندی شود:

```bash
docker compose exec web python manage.py send_appointment_reminders --dry-run
```

خروجی `--dry-run` نشان می‌دهد چه پیامک‌هایی ارسال می‌شد، بدون اینکه چیزی بفرستد
یا اعتبار پیامک مصرف کند. وقتی خروجی درست بود، cron را اضافه کن (روی خود سرور،
نه داخل کانتینر):

```bash
crontab -e
```

و این خط را اضافه کن (هر ۵ دقیقه):

```
*/5 * * * * cd /path/to/project && docker compose exec -T web python manage.py send_appointment_reminders >> /var/log/noobatyar-reminders.log 2>&1
```

نکته‌ها:
- هر نوبت فقط یک‌بار یادآوری می‌شود (فیلد `reminder_sent_at`).
- اگر جاب مدتی متوقف باشد، یادآوری‌های قدیمی‌تر از ۲ ساعت ارسال **نمی‌شوند** تا
  بعد از یک قطعی، انبوهی پیامک بی‌موقع فرستاده نشود.
- هر پیامک از سهمیه‌ی پلن صاحب کسب‌وکار کم می‌شود.

## اختیاری — رفع مشکل ۵۰۲ فرانت‌اند
اگر لینک‌های کسب‌وکار (`/b/...`) یا صفحه‌ی اصلی سایت خطای ۵۰۲ Bad Gateway می‌دهند
(یعنی کانتینر frontend پاسخ نمی‌دهد)، این را هم اجرا کن — **ولی چون کاربر گفته فقط بکند
را می‌خواهد، این بخش را فقط در صورت نیاز واقعی اجرا کن:**
```bash
docker compose logs frontend --tail 100
docker compose up -d --build frontend
```

## نتیجه را کجا گزارش بده
خروجی مرحله‌ی ۹ (تست نهایی curl) را کپی و برای تأیید نهایی ارسال کن.
